## Objective
Deploy the two existing edge functions to Lovable Cloud without any code changes.

## Functions to deploy
1. `create-booking` (`supabase/functions/create-booking/index.ts`)
2. `get-availability` (`supabase/functions/get-availability/index.ts`)

## Verification
After deployment, confirm both functions are live via a quick health/availability smoke test.
