; Several UI deps (next-themes 0.3, vaul 0.9, react-day-picker 8) declare
; peer react <=18 but work fine on React 19. The app was originally built
; with bun, which ignores peer ranges. Keep npm behaving the same.
legacy-peer-deps=true
